export const rosterQueryKey = ["roster"] as const

export const rosterImportQueryKey = ["roster-import"] as const

export const ACCEPTED_MIME_TYPES = ["text/csv", "application/vnd.ms-excel", "text/plain"] as const

export const ACCEPTED_FILE_EXTENSIONS = [".csv"] as const

export const MAX_ROSTER_FILE_SIZE = 5 * 1024 * 1024
